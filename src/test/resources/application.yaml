spring:
  datasource:
    url: jdbc:postgresql://localhost:9000/test_db
    password: 123
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true